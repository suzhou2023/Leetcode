function monotoneIncreasingDigits(n: number): number {
    let first = n.toString[0]
    let min = ''
    for (let i = 0; i < n.toString.length; i++) {
        min += first
    }
    if (n < parseInt(min)) {

    }


    return first
}


{
    console.log(monotoneIncreasingDigits(8098776679))
}